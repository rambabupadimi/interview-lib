import { CustomformatPipe } from './customformat.pipe';

describe('CustomformatPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomformatPipe();
    expect(pipe).toBeTruthy();
  });
});
